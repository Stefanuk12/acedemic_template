# Introduction

Minimal body for the anonymous-mode fixture. The real author name in
metadata MUST be rewritten by filters/03-anonymous.lua in the rendered
LaTeX output. No `authors:` list is configured, so filters/07-authors.lua
does not run and cannot mask the rewrite.

It cites [@fixture2026] to exercise citeproc end-to-end.

# References
