# Introduction

A single-paragraph fixture document used by `nix flake check` to
exercise the `lib.mkReport` public API. If the bundled filter set,
Makefile, or `mkReport` arg surface regresses, this build fails.

It cites [@fixture2026] to exercise citeproc end-to-end.

# References
